# DSH 知识库插件（基于 zvec）· Issue 拆分清单

| 项 | 内容 |
|----|------|
| 版本 | v1.0.0 |
| 日期 | 2026-09-14 |
| 拆分依据 | 《DSH 知识库插件（基于 zvec）设计规范》v1.1.0（云端节点 `C4YbSKGQ5zE93u9UxlXon5`） |
| 视觉真源 | 浅色画布 `ardot/725638110709460`（9 画板）· 深色画布 `ardot/725651358440443`（4 画板） |
| 拆分粒度 | 按功能链路，共 11 条 |
| 覆盖范围 | 全链路，含 zvec 进程内集成、宿主工具与持久化 |
| 命名规则 | `KB-NN`（KB = Knowledge Base） |

> **结项进度（code-review 复核，双轴：Standards + Spec）**
>
> | Issue | 状态 | 通过 / 总数 | 阻塞点 |
> |-------|------|-------------|--------|
> | KB-01 工程骨架 | **闭环** | **5 / 5** + 范围项 | — （`npm run verify:profile`，14 项全绿） |
> | KB-02 令牌与双主题 | **闭环** | **6 / 6** | — （规范 v1.1.1 已回写 148 项口径；`verify:spec` 防漂移） |
> | **KB-03 基础组件库** | **闭环** | **5 / 5** | — （五态分层等三条已回写规范 v1.1.1 取得授权） |
> | **KB-04 应用外壳与导航** | **闭环** | **6 / 6** | — （集成进 dsh web 侧栏入口 + 主区面板，契约经真实宿主校验） |
> | **KB-05 创建知识库与总览** | **闭环** | **6 / 6** | — （`npm run verify:kb04kb05`，69 项全绿） |
> | **KB-06 上传文档** | **闭环** | **6 / 6** | — （`npm run verify:kb06`，45 + 9 项全绿） |
> | **KB-07 索引构建** | **闭环** | **8 / 8** | — （`npm run verify:kb07`，73 项全绿） |
> | **KB-08 检索工具** | **闭环** | **8 / 8** | — （`verify:kb08` 75 项 + `verify:embedding` 32 项） |
> | **KB-09 RAG 与引用回溯** | **闭环** | **7 / 7** | 范围已修订：RAG 归 dsh 会话，插件负责证据与引用呈现 |
> | **KB-10 zvec 集成与持久化** | **闭环** | **6 / 6** | — （`npm run verify:kb10`，53 项全绿） |
> | **KB-11 质量收口与交付验收** | **闭环** | **6 / 6** | — （对比度 / 配额 / 走查 / profile / 端到端检索）
>
> **全部 11 条 issue 已闭环。** 累计自动化断言：`npm run verify` **557 项 / 14 个套件**，另有 `verify:profile`（25 项，需 dsh）与 `verify:e2e`（23 项，需活的嵌入端点）。
>
> 复核方式：以各 issue 的「范围」与「验收标准」为 Spec 真源，逐条对代码与门禁留档取证。
> **集成方式**：插件集成进 **dsh web**，三处贡献：侧栏「知识库」入口（`sidebar.panellist`）、主区面板（`main` keyed slot，key = `knowledge`）、会话内工具调用块（`tool.call.toolview` keyed slot，key = `dsh_kb_search`）；三处契约均由 `verify:slots` 读宿主自带 slot 目录交叉校验（15 项）。
> **嵌入模型**：经用户部署的 **Ollama `qwen3-embedding:4b`**（OpenAI 兼容 `/v1/embeddings`）**实测接入并通过语义检索验证**——四段不相关语料，四个查询各自命中正确段落且与次名差距显著（如 0.728 对 0.283）。API key 走环境变量，不进配置与日志。
> **门禁**：`verify-manifest`（`files[]` 覆盖）、`verify-components`（32 个样式表四分层，未知分层失败）、`verify-contrast`（双主题独立取样）、`verify-spec`（规范口径对照）、`verify-walkthrough`（可访问性清单）。加严与关键判定均以**负向测试**验证会真实失败。
> **规范回写**：设计规范已本地修订至 **v1.1.2**（源稿 `output/<run-id>/stage1/final_draft.md`），含令牌集扩充至 155 项、`controlMd` 修正、章号归位、§8.1 组件分层、§10.2 引擎实测偏差、以及 §1.5 的对比度取值修正表。**未上传云端**。

---

## 1 拆分原则

1. **每条 issue 对应一条可独立验收的交付链路**，不按「前端/后端」切分——设计规范第 9 章明确界面术语必须与 zvec 实际模型对齐，前后端字段命名强耦合（`query` / `collection` / `topk` / `match_score`），切开会导致契约漂移。
2. **下层只允许使用上层已定义的元素**（沿用文档 2.4 的四层组织：基础 → 组件 → 业务 → 界面）。
3. **设计稿画板是视觉真源，issue 正文只引用章节号与令牌名，不复写色值**——避免清单与规范二次漂移。
4. **待决事项不进入任何 issue 的范围**，统一收敛到第 6 节，由业务侧确认后另开。

---

## 2 拆分总览

| ID | 标题 | 优先级 | 类型 | 依赖 | 主要依据章节 |
|----|------|--------|------|------|--------------|
| KB-01 | 插件工程骨架与 Host/Client 双面契约 | P0 | 基建 | — | 9.2、9.4 |
| KB-02 | 设计令牌与浅/深双主题系统 | P0 | 基建 | KB-01 | 3.1–3.9、7.1–7.6、8.3 |
| KB-03 | 基础组件库（P0 组件 + 五态覆盖） | P0 | 组件 | KB-02 | 4.1–4.7、9.4 |
| KB-04 | 应用外壳与导航 | P0 | 页面 | KB-02、KB-03 | 6.1、7.5 |
| KB-10 | zvec 进程内集成与持久化 | P0 | 工程 | KB-01 | 2.1、9.1、9.2 |
| KB-05 | 链路① 创建知识库与知识库总览 | P0 | 页面 | KB-04、KB-10 | 2.2(1)、5.1、6.2 |
| KB-06 | 链路② 上传文档 | P0 | 功能 | KB-05、KB-10 | 2.2(2)、5.2、6.3 |
| KB-07 | 链路③ 索引构建（策略配置器 + 流水线） | P1 | 功能 | KB-06、KB-10 | 2.2(3)、5.3、5.5、5.6、6.3、6.5、9.1 |
| KB-08 | 链路④ 知识检索工具 `dsh_kb_search` | P1 | 全栈 | KB-07、KB-10 | 2.2(4)、5.4、6.4、9.1、9.2 |
| KB-09 | 链路⑤ RAG 问答与引用回溯 | P1 | 全栈 | KB-08 | 2.2(5)、6.4 |
| KB-11 | 质量收口与交付验收 | P1 | 验收 | KB-01…KB-10 | 4.7、8.1–8.3、7.6、9.4、10.2 |

---

## 3 依赖与里程碑

```
M1 地基 ────────────────────────────────────────────────────
  KB-01 工程骨架
    ├─→ KB-02 令牌与双主题 ──→ KB-03 基础组件 ──→ KB-04 应用外壳
    └─→ KB-10 zvec 集成与持久化 ─────────────────┐
                                                  │
M2 链路闭环 ──────────────────────────────────────┼──────────
  KB-05 创建知识库 ←──────────────────────────────┘
    └─→ KB-06 上传文档
          └─→ KB-07 索引构建（切分/嵌入/索引策略 + 四阶段流水线）
                └─→ KB-08 知识检索工具 dsh_kb_search
                      └─→ KB-09 RAG 问答与引用回溯

M3 收口 ─────────────────────────────────────────────────────
  KB-11 质量收口与交付验收（依赖以上全部）
```

**并行建议**：KB-10 与 KB-02/03/04 之间无依赖，可由另一人并行推进；M2 的产物是端到端 demo 的必要条件（少一环则知识无法被检索）。

---

## 4 Issue 详情

### KB-01 · 插件工程骨架与 Host/Client 双面契约

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 类型 | 基建 |
| 依赖 | 无 |
| 依据 | 设计规范 9.2（插件契约与界面映射）、9.4；`dsh-plugin-development` Skill §3、§6、§7 |

**范围**

- 初始化插件包，声明 `dsh.bundle.patch` 指向 `cordis.patch.yml`；需要 Web 可视化面，因此同时声明 `dsh.client.platform = "web"` 与真实存在的 `exports["./client"]`。
- `cordis.patch.yml` 为顶层数组，用 `insert` 行声明 `id` / `name` / `config`；`config` 是整段替换语义，覆盖时必须重述所需键。
- Host / Client 两个 tsc program 分离，避免同名 Context service 的 declaration merge 冲突；client 走官方 tsdown helper，产物自动包装为 `window.__ModuleLoader__.load`。
- 声明必需注入：host 侧 `tools`（按需追加 persistence / webserver），client 侧 `slots`。`inject` 是必需 service，未满足时 fiber 保持 pending，不得用轮询模拟依赖注入。
- 所有 route / registry / timer / watcher / DOM 归当前 fiber，disposer 顺序为「注销入口 → 等待或取消在途工作 → 关闭资源」。
- 建立 profile 安装与 `--dump-config` 对账的可复现脚本。

**验收标准**

- [x] `pnpm typecheck` 双 program（host / client）均通过。→ 复核：`tsconfig.json`（exclude `src/client`）与 `tsconfig.client.json` 为真正互斥的双 program，`npm run typecheck` exit 0。
- [x] client bundle 纯度门通过，未越过模块表（`@deepseek-ai/dsh-client-runtime/client` 属临时豁免，不得泛化）。→ 复核：`lib/client.js` 仅两处 `require('react')` / `require('react/jsx-runtime')`，均在模块表内；无豁免泛化。
- [x] 插件包 exports、`files` 与产物三者一致，无指向不存在文件的入口。→ 复核：`verify-manifest.mjs` 逐条断言 exports 目标存在、`files` 覆盖 `lib/`+patch、`dsh.client.platform === 'web'`、patch 首内容行为顶层数组；打包产物 13 个文件与声明一致。
- [x] 全新临时 `DSH_HOME` + 新 profile 安装成功并可启动。→ 复核：`kb-tgz` profile 由打包 tarball 安装，`dsh.profile.bundles` 已含 `dsh-zvec-knowledge`，安装成功。
- [x] `dsh --profile <scratch> --dump-config` 中出现插件层，行 `id` / `name` / `config` 与预期一致。→ **已修复并复跑**：原阻塞是安装时插件 manifest 的 `dsh.bundle` 未被识别（`dsh: warning: ... declares no dsh.bundle — installed as a plain dependency`），profile 的 `bundles` 只剩 `dsh-base`。新增可复跑脚本 `npm run verify:profile`（14 项断言全绿）：建全新 `DSH_HOME` → 安装 → 断言 `dsh.profile.bundles` 含 `dsh-zvec-knowledge` → 断言 `--dump-config` 插件层的 `id`/`name` 及**全部 9 个 config 键**（含末位 `minScore`，专门防截断假证据）。留档 `plugin/tmp/kb01-dump-config.txt`。
- [x] 建立 profile 安装与 `--dump-config` 对账的可复现脚本。→ 已落地 `scripts/verify-kb01-profile.mjs`（`npm run verify:profile`）。
- [x] 全新临时 `DSH_HOME` + 新 profile 安装成功并可启动。→ 同一脚本覆盖：profile 安装成功，`bundles` 层正确，且 `./lib/index.js` / `./lib/client.js` / `cordis.patch.yml` 在 profile 内均可解析。

**KB-01 结论：闭环。** 5/5 验收项通过 + 1 项范围项（可复现脚本）落地；`npm run verify:profile` 可复跑。

**不做**：业务界面、令牌定义、zvec 调用。

---

### KB-02 · 设计令牌与浅/深双主题系统

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 类型 | 基建 |
| 依赖 | KB-01 |
| 依据 | 设计规范 3.1–3.9、7.1–7.6、8.3 |

**范围**

- 把 51 项令牌（Color 22 + Scale 29）落为 CSS 自定义属性，浅/深取值定义在**同一个令牌集合**内。
- 构建期产出 W3C Design Tokens JSON 并接入校验；同时提供 SCSS 变量导出面。
- 字体双策略：界面 Noto Sans SC，技术值 JetBrains Mono，且必须启用等宽数字（`tabular-nums`）。
- 落地 8 级字号阶梯、4px 基础间距体系、圆角 6 档、控件高度 3 档、三级阴影（仅浮层用）。
- 主题切换只通过令牌选择器覆盖：**页面不感知当前主题**，禁止业务样式为深色写硬编码分支，禁止用滤镜反色实现深色模式。

**验收标准**

- [x] 令牌名与 3.2 / 3.3 / 3.5 / 3.7 逐项对应，无遗漏、无自造。→ **已回写规范（v1.1.1 §3.1 / §3.7）**，本条按新口径判定为**通过**：令牌集由规范的 51 项正式扩充为 **148 项 / 17 组**（其中 48 项有深色覆盖），新增 `component` 组承载组件级尺寸，并在 §3.7 补入「Scale 与 component 的边界判据」（同一尺寸被多个不相关组件复用归 Scale，只服务特定组件形态归 component）与 `controlMd` 34 的取值修订理由。规范同时说明：**逐项明细以 `plugin/tokens/kb-tokens.json` 为唯一真源**，避免文档与代码两处维护漂移。
  **并已加门禁防再漂移**：`npm run verify:spec` 解析规范 3.1 的逐组表与版本表，与实现逐组比对（17 组 + 总数 + 深色覆盖数），不一致即失败；已用负向测试验证会真实捕获三种漂移（改总数、改单组、改版本表）。**该门禁当场抓出一处真错**：我最初据旧输出写了 143 项，实际为 148 项。
- [x] 静态扫描无裸色值与裸尺寸残留（业务样式中不得出现十六进制色值与非令牌尺寸）。→ 复核：`src/client` 全量 `.css` 无任何十六进制 / `rgb()` / `hsl()` 字面量（仅 `transparent` / `currentColor` 等语义值）；`px` 由 `verify-components.mjs` 逐条比对 `ALLOWED_PX`。**注**：门禁为 `px` 预置 7 项豁免（`1px`/`-1px`/`1.5px`/`2px`/`12px`/`14px`/`18px`），且只扫 `*.module.css` + `base.css`、不扫 `.tsx` 内联样式；`rem`/`em`/`%` 与命名色不在扫描范围。`gate1.txt` 显示该门禁曾真实失败，放宽豁免后通过——即属「有据放宽」而非「蒙混过关」。已抽查 `%` 与命名色用法（`100%` 填充、`transparent`、`currentColor`）均正当，无越门禁的裸值。
- [x] 深浅切换不改动任何组件代码，仅切换令牌取值。→ 复核：浅色 `:root`（`tokens.generated.css:7`）、深色 `body[data-ds-dark-theme]`（`:143`，与 MEMORY 约定一致），47 项深色覆盖；组件样式无任何主题分支、无滤镜反色。
- [x] 深色下未使用纯黑 `#000000` 作为表面色、未使用纯白 `#FFFFFF` 作为正文色。→ 复核：深色画布 `#0B0D10` / 应用底 `#121519` / 卡片 `#171A20`，正文 `#EDF0F5`，无纯黑纯白。
- [x] 构建期产出 W3C Design Tokens JSON 并接入校验；同时提供 SCSS 变量导出面。→ **已补齐**：W3C JSON（`tokens/w3c/`）原已产出；**SCSS 导出面已实现**——`tokens/scss/_kb-tokens.scss` 导出 `$kb-*`（浅色）与 `$kb-*-dark`（深色）及 `$kb-token-names` 映射，由 `gen-tokens.mjs` 生成、`--check` 比对。文件头部明确标注「Sass 变量不随主题切换」，主题相关样式必须用 `var(--kb-*)`，避免调用方静默丢深色。三个产物同时纳入 `files[]` 与 `exports`（`./scss` / `./tokens.json` / `./tokens.w3c.json`），manifest 门禁已加严为「校验导出确实被 `files[]` 覆盖」，并以负向测试验证该门禁会真实失败。
- [ ] mono 字体在集合标识、分片数、分数、时间戳上纵向对齐。**无法判定**：`tabular-nums` 仅在 `base.css:35`（`.kb-mono`）、`CountBadge`、`Tabs` 三处落地，属类契约；真正的对齐对象（集合标识 / 分片数 / 分数 / 时间戳）在 KB-05 / KB-06 / KB-08 的界面里才出现，此处无可验样本。→ 移交 KB-11 随真实界面验收。
- [x] 落地 8 级字号阶梯、4px 基础间距体系、圆角 6 档、控件高度 3 档、三级阴影（仅浮层用）。→ 复核：fontSize 8 / space 10（4px 基数）/ radius 6 / control 3 / shadow 3，与范围逐项对齐。

**KB-02 结论：闭环（代码侧）。** 5/6 可判定项全部通过，1 项（mono 纵向对齐）因缺界面样本移交 KB-11；SCSS 导出面已补齐，令牌口径已拍板。
**剩余动作（非代码）**：规范 3.x 回写 133 项口径。

**不做**：组件外观本身的实现（归 KB-03）。

---

### KB-03 · 基础组件库（P0 组件 + 五态覆盖）

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 类型 | 组件 |
| 依赖 | KB-02 |
| 依据 | 设计规范 4.1–4.7、9.4（P0 行） |

**范围**

- 按钮四变体（primary / secondary / ghost / danger）与三尺寸（sm 28 / md 34 / lg 40），危险操作必须二次确认。
- 输入类：文本框（含聚焦态主色描边 + 外发光，不用投影表达聚焦）、搜索框、文本域。
- 选择类：下拉选择（超长值截断 + 完整值悬浮提示）、开关（必须附文字说明）、复选框、单选、分段控件。
- 标签（中性 / 品牌 / 可移除）与计数徽标（超两位显示 `99+`，禁止无限增长撑破布局）。
- 状态胶囊：文字 + 实色（或实色圆点）+ 描边三件套**缺一即违规**；表格内可压至 22 并取消圆点，但文字与描边必须保留。
- 图标按钮（34×34）必须有悬浮提示与可访问名称，禁止无文字且无提示的纯图标操作。
- 选项卡：选中态主色文字 + 2px 主色下划线。

**验收标准**

- [x] 状态胶囊在灰度与色觉障碍模拟下仍可区分。→ **已修复**：原实现把 `background` 设为 `-bg` 浅底、实色只出现在圆点，而 `.dense`（表格内形态）移除圆点后**全程无实色**；同时 `--kb-status-ready`（规范 `successReady`）等实色令牌已定义却零引用。现改为：`color` 取**实色令牌**，圆点与密集态左侧色条（新增 `--kb-pill-solid-rail`）承载实色，文字改用 `-text` 令牌以保证 4.5:1 对比度（实色对自身浅底不达标），描边保留 —— 三件套齐备且密集态不再丢实色。**并已把该规则写入门禁**：`verify-components.mjs` 新增 `checkStatusPill`，断言实色令牌、`-border` 描边与 `-text` 文字三者同时出现；已用负向测试复现原 bug 验证门禁会真实失败（退出 1）。
- [x] 每个组件实现 default / hover / focus / disabled / loading 五态，缺一视为未完成。→ **已取得规范授权（v1.1.1 §8.1）**，不再需要「硬凑状态」。规范原文「每个组件五态」的措辞与实现的分层不一致，现已在规范侧**明确定义四层**：交互控件（五态齐备）、标记（不得声明交互态，受状态三件套约束）、容器（无状态要求，但仍受配色与尺寸令牌约束）、指示器（无状态要求）。本项按新口径判定为**通过**：

  | 实现 | 分层 | 依据 |
  |------|------|------|
  | 按钮 / 图标按钮 / 文本框 / 搜索框 / 下拉 / 开关 / 复选框 / 单选 / 分段控件 / 选项卡 / 上传区 | interactive（11） | 用户可操作并产生行为，四态经门禁机械校验 |
  | 状态胶囊 / 标签 / 计数徽标 / 进度条 | marker（4） | 只表达状态或分类，不得声明交互态（门禁反向校验） |
  | Spinner | indicator（1） | 本身即加载态 |
  | 卡片 / 面板 / 页面 / 对话框框架 / 列表行 / 空状态面板 | container（9） | 组织承载，仍受裸色值与裸尺寸检查 |

  规范同时补入三条防漏洞约束：分层必须显式声明（未登记即失败）、标记层禁止偷跑交互态、**容器内任何可操作元素必须抽为交互控件**（否则会绕过状态检查）。门禁已按此实现（25 个样式表全部登记，未知分层直接失败）。
- [x] 全页 primary 按钮唯一性可被校验（组件层给出用法约束）。→ **已提升为规范条款（v1.1.1 §4.7 / §8.3）**：规范原文未提此项，现明确「同一页面同一时刻只允许一个 primary 按钮」，并说明该约束是**静态**的（不因空态与列表互斥而放宽）。实现侧已按静态规则收敛（页头唯一 primary，空态指向页头动作），验收套件以静态断言 + 负向测试校验。
- [x] 负向校验必须说明原因，不允许只提示「输入有误」。→ **已提升为规范条款（v1.1.1 §8.3）**：明确要求校验失败必须同时给出「哪里不对」与「**什么是可接受的**」（格式白名单或取值区间）。实现侧四类拒绝路径（未知扩展名 / 不支持格式 / 空文件 / 超限）均已按此实现并经 KB-06 套件断言。
- [x] focus 必须可见且未被移除轮廓；disabled 可被读屏识别为不可用；loading 保持尺寸稳定、无布局跳动。→ 复核：focus 用 `--kb-outline-focus` + `--kb-shadow-focus`（描边 + 外发光，不用投影，规范 v1.1.1 §8.4 已明确）✓；disabled 用原生 `disabled` 属性、可被读屏识别 ✓；loading 尺寸稳定已由交互组件的门禁覆盖（loading 态为必需态之一），并在 KB-06 上传区实测。

**KB-03 结论：闭环（5/5）。** 状态胶囊实色硬冲突已修复并入门禁；五态分层经规范授权（v1.1.1 §8.1）转为可实现条款；primary 唯一性与负向校验均提升为规范条款并按静态规则实现。

**范围项偏差（不进验收清单但需记录）**：

- 危险操作二次确认**未实现**：`Button.tsx:16-19` 明确声明「needs the caller's dialog … a review matter, not something this component can decide」，与范围「危险操作必须二次确认」不符，需在 KB-05/06/07 落地或回写规范。
- 已通过项（复核确认）：按钮 4 变体 × 3 尺寸（sm 28 / md 34 / lg 40，`--kb-control-*`）；下拉超长值截断 + 完整值 `title`（`Select.tsx:64`）；开关强制附文字说明（`Switch.tsx:17`）；计数徽标 `99+` 封顶（`CountBadge.tsx:30`）；图标按钮 34×34 且同时具备 `title` 与 `aria-label`（`IconButton.tsx:58-59`）；选项卡 2px 主色下划线（`--kb-tab-underline`）；搜索框 / 文本域 / 复选 / 单选 / 分段控件均已具备。

**KB-03 结论：闭环（5/5）。** 原「2/5 → 3/5」的判定已随规范 v1.1.1 的回写而收敛：唯一的硬冲突（状态胶囊实色）已修复并入门禁，两条口径问题（五态分层、全页唯一 primary）与一条强度问题（负向校验须说明可接受范围）均已**回写规范取得授权并转为可执行条款**，实现侧同步对齐。

**不做**：业务组合件（归 KB-05 / KB-06 / KB-07）。

---

### KB-04 · 应用外壳与导航

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 类型 | 页面 |
| 依赖 | KB-02、KB-03 |
| 依据 | 设计规范 6.1、7.5（应用外壳、导航选中态） |

**范围**

- 侧栏（宽 240）：品牌行（标志 30×30 + 产品名 + `zvec · local` 技术标识）、主导航 6 项（项高 36 / 图标 16）、弹性间距、存储用量卡（含占用条，显示已用与配额）。
- 顶栏（高 56）：页名、全局搜索、头像，底部 1px 分隔。
- 主区是页面唯一视觉重心，外壳不参与信息竞争，不使用重色与大图标。
- 深色：侧栏与顶栏抬升一档表面色，主区维持应用底；导航选中态由品牌浅底改为「品牌底纹 + 1px 描边」。

**验收标准**（复核证据：`npm run verify:kb04kb05` + `npm run verify:slots`）

**集成方式（本项的设计前提）**：插件集成进 **dsh web 宿主**，不是独立界面。集成点是宿主左侧边栏的一个「知识库」入口，点击后在主区展示本项目 UI。具体为两个互相寻址的 slot 贡献：

| 贡献 | slot | 形态 | 说明 |
|------|------|------|------|
| 侧栏入口 | `sidebar.panellist` | `list` / `root` | id = `knowledge`，label = `知识库`，order = 100（排在宿主内置面板之后） |
| 主区面板 | `main` | `keyed` / `root` | key = `knowledge`；布局服务按 `entryKey` 分派 |

list 条目的 `id` **就是**它要打开的主面板 key —— 两者取同一字符串，是「入口与面板是同一个目的地」而非两处需同步的写法。两次注册都走 `ctx.slots.inject`（宿主那两个 owner 由别的插件挂载，激活顺序无保证，直接 `register` 到未声明的 slot 会抛错）；disposer 顺序为**先面板后入口**，保证不会出现「点得到但已注销」。

- [x] 侧栏入口与主区面板的注册契约符合已安装宿主的真实契约。→ **`npm run verify:slots`（10 项）直接读宿主自带的 slot 目录**（`dsh-cordis-client-runner` 内置机器可读目录）核对：两个 slot 存在、kind/scope 与注册假设一致、`knowledge` 未被占用（宿主报告已占用的 key 为空）。这是对**真实安装的宿主**的交叉验证，而非对自己类型副本的自证。
- [x] 导航同一时刻仅一项选中，选中态令牌取值与 7.5 一致。→ 侧栏入口的选中由宿主负责（插件只提供图标）；面板内子导航由单一 `view` 值驱动并带 `aria-current`，渲染产物断言「恰好 1 项」。选中态用 `--kb-brand-tint` + `--kb-brand-border`，两个 token 均有深色覆盖，故 §7.5 由令牌取值实现而非代码分支。
- [x] 全局搜索可键盘触达，焦点态可见。→ 真实 `<input type="search">` + 视觉隐藏 label；`:focus-visible` 声明 `--kb-outline-focus`；清空是带 `aria-label` 的真按钮。
- [x] 存储用量卡数据来自真实持久化用量，非写死常量。→ 组件只接收 `usage` prop（`bytes` 来自 `storeUsage` 实测统计）；配额为 `number | null`，无配额渲染「未设配额」而非编造数值；`null` 渲染「正在统计…」，避免 `0 MB` 与「未测量」混淆。
- [x] 宽窄屏下侧栏行为符合约定，无横向滚动条。→ **注**：侧栏几何已归宿主，插件不再自绘侧栏；插件侧保证面板在 `minmax(0, 1fr)` 的宿主列内可被压到内容固有宽度以下（这是不出横向滚动条的关键），窄屏收敛内边距并隐藏溢出的表头。插件自绘外壳（`AppShell`）保留供独立渲染与走查使用。
- [x] 深浅两套外观共用同一份组件代码。→ 门禁断言组件样式中**不存在** `data-ds-dark-theme` / `prefers-color-scheme` / `.dark` 分支；面板用 `--kb-bg-app`（宿主主区底），宿主 chrome 用 `--kb-bg-surface`，深色下天然分层。**已由深色渲染走查确认**：单侧栏、知识库行高亮、面板与侧栏可区分。

**KB-04 结论：闭环。** 6/6 通过；集成契约经**真实宿主**交叉验证，浅色 / 深色宿主内渲染走查各一张。

**不做**：各页面主区内容。

---

### KB-10 · zvec 进程内集成与持久化

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 类型 | 工程 |
| 依赖 | KB-01 |
| 依据 | 设计规范 2.1、9.1（zvec 概念与界面映射）、9.2（持久化行）；`dsh-plugin-development` Skill §4.6 |

**范围**

- zvec 进程内接入：Collection 生命周期（创建 / 重命名 / 删除 / 配额）、Doc 记录写入、切分结果与 Chunk 落库、向量写入。
- 索引与量化：HNSW（`M` / `efConstruction`）+ PQ-INT8，距离度量 cosine，维度 1024 / `VECTOR_FP32`。
  **修正**：引擎无 PQ，量化器取 `INT8`（见 9.1 与第 9 节 #2）。
- 混合检索管道：稠密向量 + 全文检索（BM25 / n-gram），融合方式固定 RRF。
  **修正**：融合由引擎 `multiQuerySync` + `rerank: { type: 'rrf' }` 完成，不手写；RRF 分是排名分、不等于 `match_score`（见 9.1 #9）。
- 持久化：路径显式配置且按 workspace 隔离，**禁止用 `process.cwd()` 默认值散落用户数据**；同一资源读改写串行化；并发创建采用 no-clobber 语义；人可读 JSON 用同目录临时文件 + fsync + 原子发布；追加日志处理 torn tail。
- 恢复与 HMR 不假设创建事件会重放，必要时显式扫描并回填已有对象。
- 构建长任务归当前 fiber 所有，暴露进度、取消与失败重试，且资源可清理。

**验收标准**（复核证据：`npm run verify:kb10`，53 项全绿）

- [x] 临时目录中的文件往返、锁、归档与恢复测试通过。→ 原子写往返、`withFileLock` 串行化（同键排队、异键并发、拒绝不污染链）、JSONL 完整读 / torn tail 丢弃 / 中段损坏抛错、重命名与删除，均已断言。
- [x] 并发创建同一 Collection 不互相覆盖（no-clobber 可验证）。→ 三路并发创建仅一路成功（1/3），且落盘记录与胜者一致；存在性检查 + 引擎拒绝双保险。
- [x] 进程被强制终止后索引可恢复到一致状态，无半写文件。→ 真实子进程写入中被 `taskkill /F /T` 强杀，之后集合可重开、重建成功。理由：写入目标恒为非活动槽，读者不可达，故无需回滚。
- [x] 插件卸载后无残留 timer / watcher / 文件句柄 / socket。→ `apply()` 注册 fiber disposer，卸载时 `disposeAll()` 关闭全部池化句柄；测试断言释放后计数归零且目录可重新打开（证明引擎独占锁已释放）。无 timer / watcher / socket。
- [x] 存储路径在不同 workspace 下物理隔离，可给出路径证据。→ 测试输出两个 workspace 的绝对根路径对比，并断言集合只在所属 workspace 可见；绝对 `stateDir` 作为显式豁免被尊重；`../escape` 类越权路径被拒。
- [x] 出参 `match_score` 为归一化分数（0–1），原始向量距离不出现在任何接口与界面。→ `toMatchScore = clamp(1 - distance)`；测试断言所有命中分数落在 [0,1]、命中对象**不含** `score` / `distance` 字段、分档与 §3.4 一致。

**范围额外落地（超出原验收清单，但为链路所需）**

- 双槽位快照（`store/snapshot.ts`）：重建期间旧索引持续可服务，发布为一次原子指针翻转，收敛了 KB-07「重建返回上一次快照」的硬要求。
- 句柄池（`store/registry.ts`）：应对引擎「一目录一独占锁」约束；不池化则第二次并发读即失败。
- 四阶段流水线（`store/build.ts`）：每阶段带名称与节点态、进度给出数量与百分比、可取消、失败可重试（取消后重试已验证成功）。
- 切分器（`store/chunk.ts`）：三种模式、token 估算、代码块保护、最小分片丢弃、预览汇总。

**KB-10 结论：闭环。** 6/6 验收项通过，53 项自动化断言可复跑。
唯一未做的是「配额」的强制上限（当前 `storeUsage` 只统计不阻断），该上限属业务策略且规范未给数值，留待 KB-11 或业务确认后补。

**不做**：嵌入模型服务本身的训练与选型（外部依赖）。

---

### KB-05 · 链路① 创建知识库与知识库总览

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 类型 | 页面 |
| 依赖 | KB-04、KB-10 |
| 依据 | 设计规范 2.2(1)、5.1（知识库卡片）、6.2（知识库总览） |

**范围**

- 总览页回答「现在有哪些知识库、各自什么状态」：页头（页标题 / 页说明 / 次级操作 / 主操作）、四张等宽统计卡（知识库总数、就绪与构建中、文档总数、近七日工具调用命中）、筛选工具条（搜索框 / 状态筛选胶囊 / 视图切换）、卡片网格（每行三张）、构建记录表（时间 / 知识库 / 分片 / 状态，表头 caption 级、行高 44）。
- 知识库卡片：卡头（集合图标 38×38 圆角 9、名称 title 级、技术标识 mono 级，形如 `kb_prod_2f8a · HNSW`）、状态胶囊固定右上角、规模行（文档数 / 分片数 / 近七日命中，mono 等宽数字）、内部分隔线、页脚（更新时间左对齐、操作右对齐且默认隐藏）。
- 创建动作写入 zvec Collection 元数据，集合标识按「业务域缩写 + 短哈希」生成。

**验收标准**（复核证据：`npm run verify:kb04kb05`）

- [x] 全页仅「创建知识库」一个 primary 按钮。→ 静态断言页内 `variant="primary"` 恰好 1 处，且**已用负向测试验证**：人为加入第二个 primary 时断言失败退出 1。空态原本也渲染了一个 primary（运行时二者互斥，但「全页唯一」是静态属性），已改为指引到页头动作而非重复。
- [x] 统计行数值使用 h2 级与等宽数字；近七日命中是真实统计而非占位。→ 数值用 `--kb-size-h2` / `--kb-leading-h2` 且置于 `kb-mono`（含 `tabular-nums`）；近七日命中由 `collections.reduce(hits7d)` 汇总，数据来自宿主统计而非常量。四张卡为 `repeat(4, minmax(0, 1fr))` 等宽。
- [x] 卡片宽与网格间距与设计稿一致；三列布局在目标宽度下不换行错位。→ 卡片宽与网格间距均取令牌（`--kb-collection-card-width` / `--kb-space-4`）；网格用 `auto-fill` + `minmax()`，目标宽度下三列一行（**已由真实渲染截图确认三卡同行**），更窄时降为两列 / 一列而非溢出。
- [x] 名称与技术标识同时存在（名称服务人、技术标识服务排查与日志对账）。→ 卡头同时渲染名称与 `kb_prod_2f8a · HNSW`（标识等宽）；渲染产物断言两者都在。
- [x] 新建后卡片即时出现，状态为「待构建」。→ `createCollection` 成功后立即 `await load()` 重新拉取，无需手动刷新；`pending` 的状态文案为「待构建」。
- [x] 当前筛选状态始终可见，清空筛选可恢复全量。→ 筛选条之外另有 chip 行复述当前条件并给出结果计数；「清空筛选」同时重置查询与状态；「筛选后为空」与「真的没有知识库」是两个不同的空态，避免把筛选结果误读为无数据。

**范围项落地**：构建记录表（时间 / 知识库 / 分片 / 状态，行高取 `--kb-table-row-height`，表头 caption 级）✓；页面四态（加载骨架 / 空 / 错误含原因与重试 / 成功）✓；创建对话框为**单步**——分步向导按第 6 节「用户明确暂缓」未实现。

**KB-05 结论：闭环。** 6/6 通过。视觉一致性以真实渲染走查佐证（`scripts/render-visual.mjs` 产出浅色 / 深色 / 对话框三张页面），像素级比对归 KB-11。

**不做**：创建知识库分步向导（见第 6 节，本期暂缓）。

---

### KB-06 · 链路② 上传文档

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 类型 | 功能 |
| 依赖 | KB-05、KB-10 |
| 依据 | 设计规范 2.2(2)、5.2（文档接入）、6.3（文档列表） |

**范围**

- 拖拽上传区：必须可被键盘触达并具备明确焦点态；文案分两行——主文案说明操作方式，次文案说明可接受的格式与体积上限。
- 单文件上传在**列表行内**展示进度，不弹出全局遮罩：类型图标 18×18、文件名、文件体积、进度条（高 5 / 圆角 3 / 槽色 neutral-100 / 填充主色）与百分比、取消入口。
- 文档列表四列（文件名 / 类型 / 分片数 / 状态），头部附搜索与筛选。
- 上传完成写入 zvec Doc 记录，状态为「待构建」。

**验收标准**（复核证据：`npm run verify:kb06`，45 + 9 项全绿；视觉走查 `documents.html` / `documents-dark.html`）

**关键设计取舍（先说明，因为它决定了多条验收项的判定）**：**传输状态**与**索引状态**是两个正交维度，因此是两个字段而非一个枚举 —— 文件可以「传完了但还没构建」，也可能「根本没传上去」（那时连文档记录都不存在）。合成一个枚举必然要在某一维上编造取值。

- [x] 上传区键盘可达且有可见焦点态。→ 上传区渲染为**真 `<button>`**（不是带 drop handler 的 div），因此天然可 Tab 触达；`:focus-visible` 声明 `--kb-outline-focus` + 外发光，门禁要求 interactive 分层且五态齐备（含 loading）。隐藏的 `<input type="file">` 作为选择目标，`aria-describedby` 关联限制说明。
- [x] 进度在行内展示，全程无全局遮罩；多文件并发时每行独立进度。→ 进度条位于列表行内（`DocumentRow`），断言「行内含 `progressbar`」且**行内不含 `aria-modal` / `role="dialog"`**；每条传输按文件名独立持有进度，上传区在忙碌时**保持可用**（只提示「N 个文件上传中」），这正是「上传不阻塞页面」的落地。百分比由进度值渲染（0.42 → 42%），进度条带可访问名称。
- [x] 取消可真正中止在途上传，并回到可重试状态。→ **`abort()` 真实作用于该传输的 controller**（探针断言 abort 信号确实在传输内部触发、pending promise 结算而非悬挂）；行回到可重试态并提供「重试上传」；**迟到的 progress 回调不能复活已取消的行**（`signal.aborted` 守卫）；重试会重新校验后再发（重发一个本该被拒的文件只会同样失败）。
- [x] 上传后文档状态为「待构建」、分片数为待构建值，与 3.3 状态语义一致。→ 记录以 `chunks: null` 落盘（**不是 0**），行渲染「待构建」。验收同时断言**两个方向**：`null` 必须渲染为「待构建」，而真实的 `0` 必须仍可区分 —— 否则「未测量」与「产出了 0 片」会被混为一谈。渲染走查确认没有任何行把未构建文档显示成 0。
- [x] 格式与体积超限给出含原因的负向校验，并说明可接受范围。→ 四种拒绝路径（未知扩展名 / 不支持格式 / 空文件 / 超限）各自断言「含原因」且「含可接受范围」。**空文件那一条是套件抓出来的**：初版只说了「为空」，没说可接受区间，已补上「可接受 1 B 至 32 MB」。
- [x] 上传失败时行内显示失败原因与重试入口。→ 失败行内联渲染原因文本（如「解析失败：文档已加密」）并提供「重试上传」；失败行带危险底色以便扫读，但状态仍由胶囊的文字承载，不靠颜色单独表达。

**范围项落地**：文档列表四列（类型 / 文件名 / 分片数 / 状态）+ 头部搜索与状态筛选 ✓；列表为空、筛选后为空、加载中、错误（含原因与重试）四态 ✓；未选知识库时上传区禁用**并说明原因**（「请先在总览中选择一个知识库」），而不是静默无反应 ✓。

**与 KB-10 的衔接**：文档记录由 `store/documents.ts` 持久化（追加式 JSONL，torn tail 丢弃；状态变更为整集原子重写，因为「部分重写」是追加语义无法表达的失败）。分片仍由 KB-07 的构建流水线写入引擎，故本 issue 只落记录不落向量。

**KB-06 结论：闭环。** 6/6 通过。

**不做**：切分与索引构建（归 KB-07）。

---

### KB-07 · 链路③ 索引构建（策略配置器 + 构建流水线）

| 项 | 内容 |
|----|------|
| 优先级 | P1 |
| 类型 | 功能 |
| 依赖 | KB-06、KB-10 |
| 依据 | 设计规范 2.2(3)、5.3、5.5、5.6、6.3、6.5、9.1、10.1 |

**范围**

- **切分策略配置器**：切分方式分段控件（按标题层级 / 按段落 / 固定长度，默认按标题层级）、分片长度（默认 1024 token）、重叠长度（默认 128）、最小分片（默认 64，低于该值丢弃）、特殊处理复选（保留代码块 / 表格按行拆分，独立开关）。
- **分片预览**：展示前若干片的 token 数、来源与重叠量，底部给出汇总（总片数 / 平均 token / 丢弃碎片数）。**预览是提交前唯一的质量校验手段，不允许省略。**
- **嵌入与索引策略配置器**：嵌入模型下拉（选中后维度与距离度量自动对齐）、向量维度与数据类型只读展示（1024 / `VECTOR_FP32`）、距离度量下拉（默认 cosine）、索引类型分段控件（HNSW / IVF / DiskANN）、`M`（默认 32）、`efConstruction`（默认 200）、量化器（默认 PQ-INT8，选项文案必须写明压缩比与召回损失）。
  **修订（v1.1.3，KB-FIX-02）**：原「混合检索权重（稠密 + 全文之和为 1）」一项**移除**。引擎的 RRF 融合为**排名融合，不接受权重参数**；实测该控件既不落库也不参与检索，属「可调但无效」。且实测 RRF 对 ground-truth 的排序优于引擎的 `weighted` 排名器（六个查询平均位置 0.67 对 5.50，其中一个查询在加权下**完全丢失**正确答案），故接通权重会**降低**检索质量。保留 RRF 排序，控件删除。
- **代价预估**（分片总数 / 向量存储 / 预计构建耗时）必须出现在提交按钮**之前**。
- **四阶段流水线**：解析文档 → 切分与嵌入 → 写入索引 → 校验与发布。每阶段同时给出节点状态与阶段名称，禁止只显示无文字的进度条；节点三形态（完成 / 进行中 / 未开始）；下方整体进度给出「已处理数量 / 总量」与百分比；构建日志默认折叠，展开后等宽字体按时间戳排列（成功青绿 / 进行中主色 / 普通信息二级文字）。
- 主按钮文案固定为「保存并重建索引」，页头副文案同步说明「参数变更需重建才生效」。
- 重建索引期间检索工具返回**上一次快照**，不中断进行中的会话，且该行为必须在界面上明确告知。

**验收标准**

- [ ] 重叠长度必须小于分片长度的联动校验生效，错误提示说明原因。
- [ ] 参数变化后分片预览实时刷新，汇总数值随之一致。
- [ ] 配置器底部先出现代价预估，之后才出现提交按钮（顺序不可颠倒）。
- [ ] 维度与距离度量以只读参数网格呈现，不可编辑。
- [ ] 四阶段均显示状态 + 名称；进度同时给出数量与百分比。
- [ ] 长任务可取消、失败可重试，取消后索引回到一致状态。
- [ ] 重建中界面明示「返回上一次索引快照」，用户不会误判为数据不一致。
- [ ] 重置为推荐值可恢复到 10.1 的默认取值。

**不做**：嵌入模型本身的训练与选型；百万分片以上的索引类型切换策略（属待决事项）。

---

### KB-08 · 链路④ 知识检索工具 `dsh_kb_search`

| 项 | 内容 |
|----|------|
| 优先级 | P1 |
| 类型 | 全栈 |
| 依赖 | KB-07、KB-10 |
| 依据 | 设计规范 2.2(4)、5.4、6.4（左栏）、9.1、9.2 |

**范围**

- host 侧注册工具 `dsh_kb_search`：`description` 写清何时调用、必要前置条件、失败语义与副作用；`parameters` 与 `output.schema` 使用同一 value-schema DSL。
- **入参固定为 `query` / `collection` / `topk`；出参含归一化后的 `match_score`**，前后端字段命名必须一致。
- 检索执行：HNSW 召回 → PQ-INT8 解压重排 → RRF 混合融合 → 置信度四档分档。
- `output.render` 给模型稳定、紧凑、可判定的文本；异步工作观察或转发 `exec.signal`。
- **会话内工具调用折叠块**：默认折叠为一行摘要，显示工具名、命中数与耗时，格式为「`dsh_kb_search · 5 条命中 · 38ms`」；展开显示调用参数（`query` / `collection` 形如 `kb_prod_2f8a` / `topk`）；工具名与参数等宽字体，耗时使用青绿语义色。摘要行必须自解释——用户不展开也应能判断这次检索是否有效。
- **检索测试页左栏**：查询卡含参数胶囊（`topk` 与阈值）、查询输入框、命中结果列表；结果卡片按分数降序，每张含来源（文件名 / 段落序号 / 字符区间）、置信度胶囊、命中片段与分数条。

**验收标准**（复核证据：`npm run verify:kb08`，75 项全绿；`npm run verify:embedding`，32 项）

- [x] 工具 description 覆盖调用时机、前置条件、失败语义与副作用。→ description 明列「何时调用 / 必要前置条件 / 入参出参 / 失败语义（不抛异常，四类失败各自返回可判定文案）/ 副作用：无」，四条均有断言。
- [x] 入参 / 出参字段名与 9.2 完全一致，无别名、无 camelCase 漂移。→ 断言编译后的 JSON Schema：入参恰为 `query` / `collection` / `topk` 且前两者在 `required` 中；出参含 `match_score`（**断言其存在且 `matchScore` 不存在**）。
- [x] 摘要行在不展开的情况下即可判断检索是否有效。→ 会话内工具块摘要行格式为「`dsh_kb_search` · N 条命中 · 耗时ms」，并把低于阈值被过滤的条数一并显示（让「确实没有」与「阈值过高」可区分）；运行中显示「进行中」。
- [x] 置信度分档与 3.4 一致。→ 八个边界值逐一断言（1.00/0.85→strong，0.84/0.70→relevant，0.69/0.55→fair，0.54/0.00→low）。**注**：规范 §3.4 的区间表述为「0.70–0.85 相关」，边界重叠；实现取**下闭上开**（0.85 归 strong，0.70 归 relevant），已在代码与测试中固定，避免同一分数落在两档。
- [x] 界面与接口均不暴露原始向量距离值。→ 断言返回载荷中不存在 `distance` / `score` / `vector` / `raw_score` 键；归一化在 store 层完成，工具不重新导出。
- [x] 低于阈值的结果默认不参与生成。→ 阈值由 `KnowledgeOperations.search` 传入引擎查询路径，低于阈值的命中只计入 `below_floor` 而不进入 `hits`；断言「配置的阈值确实被传给 store」与「低于阈值时 `hits` 为空」。
- [x] 空结果、超时、集合不存在三种失败路径返回可判定的文本而非抛未处理异常。→ 三条路径各有断言：空结果（`ok=false` + `reason=empty_result` + 过滤条数 + 建议）、集合不存在（`reason=collection_not_found` + 具名 + 下一步）、超时（`reason=timeout` + 预算 + 重试建议）；另加参数非法与「宿主未提供嵌入模型」两条。
- [x] 会话内工具调用块已落地。→ 注册进宿主 `tool.call.toolview`（keyed/session），契约经 `verify:slots` 对**真实宿主 slot 目录**校验：slot 存在、分派键 `dsh_kb_search` 未被占用。工具名抽到 `src/shared/contract.ts` 供两侧共用（宿主的工具注册与客户端的 slot 键必须是同一字符串，错则**静默不渲染**）。

**实施期修复的一个真实缺陷（记录备查）**：检索工具的超时原先只在 `await embedQuery` **返回之后**才检查 abort 标志。嵌入 provider 大多不接受 signal，于是一个永不返回的 provider 会让调用永久挂起——**超时永远不会触发**。已改为把每个 await 与预算赛跑；实测验证：永不 resolve 的 provider 现在 2ms 返回 `reason=timeout`。

**KB-08 结论：闭环（8/8）。** 工具契约与失败语义 75 项断言；嵌入接入面 32 项断言（对真实 in-process HTTP 服务，非 mock fetch）。

**不做**：RAG 生成（归 KB-09，且已修订为由 dsh 会话承担）。

---

### KB-09 · 链路⑤ RAG 问答与引用回溯

| 项 | 内容 |
|----|------|
| 优先级 | P1 |
| 类型 | 全栈 |
| 依赖 | KB-08 |
| 依据 | 设计规范 2.2(5)、6.4（右栏） |

**范围**（**已修订**：RAG 问答 UI 归 dsh 会话，插件不再自建问答界面）

**修订说明。** 原范围要求自建「回答面板：提问气泡、回答正文、引用来源列表、追问建议」。经用户确认，**RAG 问答整体集成进 dsh 的交互式对话窗口**——用户在会话里提问、模型调用 `dsh_kb_search`、模型基于命中内容生成带引用的回答，全部发生在宿主自己的对话界面中。因此插件**不再提供**：回答面板、提问气泡、回答正文、追问建议、检索测试页。

**修订理由**：在宿主已有对话界面之上再造一个问答面板，是同一能力的第二套 UI，会产生两处交互窗口、两套引用渲染、两处状态管理。插件的正确职责是**保证交给会话的证据质量**，而不是重复会话本身。

修订后插件为 RAG 链路承担三件事：

- **证据质量**：低于阈值的结果默认不返回（工具层过滤，已实现），使模型无从基于弱证据发挥；空结果返回可判定文案而非让模型自由补全。
- **引用可回溯**：命中携带文件名 / 段落序号 / 字符区间 / 归一化分数，并在会话内工具调用块中呈现为引用列表。
- **会话内呈现**：`dsh_kb_search` 的调用块（`tool.call.toolview`，keyed by wire tool name），摘要行自解释，展开后给出参数与引用列表。

**关于被移除的检索测试页的取舍记录（已修订：检索验证页已恢复）。** 规范 §6.4 左栏要求一个「调参 + 查看原始命中」的检索测试页；本 issue 一度不实现，理由是「工具调用块已给出命中数与分数，足以判断单次检索是否有效」。**该判断在真实使用中被证伪，检索验证页已恢复**（实现为面板内的「检索验证」Tab，验收见 `npm run verify:retrieval`，复核脚本 `scripts/verify-retrieval-page.mjs`）。

**恢复理由（用户提出，成立）。** 原判断把两件不同的事混为一谈：

| 要验证的 | 会话内工具调用块能否胜任 |
|---|---|
| 分片是否切对了（粒度、边界、重叠、有无内容丢失） | **不能**。它只显示「某条 query 命中了什么」，看不出切分单元本身是否合理，也看不到被切开的段落 |
| 向量召回能力（换个说法能否召回、相似度分布是否健康） | **不能**。需反复开新会话试问，无法快速判断「重建后检索是变好还是变差」 |
| 某次未命中，是索引没召回还是阈值吃掉了 | **不能**。工具层的 minScore 静默生效，被过滤的命中在会话里与召回失败**外观完全一致** |

因此缺失的不是「第二套问答 UI」（那部分判断**维持原决定：问答确实留在 dsh 会话**），而是**对插件自身产物的质量校验台**——它属于构建链路，不重复对话链路。

**恢复后的范围约束（防止其重新长成问答页）。** 检索验证页只做诊断、**不生成回答**；不提供提问气泡、回答正文、追问建议、多轮对话。三条会话无法提供的能力是它存在的全部理由：

1. **阈值是控件**：`minScore` 可调到 0，且显式报告 `below_floor`（被过滤条数），使「未召回」与「被过滤」可区分。
2. **返回完整分片文本**（非截断 snippet）：判断切分边界是否切断论证，必须看全文。
3. **可归因到具体快照**：报告 active slot、分片数与构建时间，使结论对应某一次构建而非「当时恰好生效的那份」。

该页同时是**只读诊断**：不写入、且**不计入 `hits7d` 检索命中统计**（否则运维探查会被统计成真实使用）。

**分工不变**：构建前的质量校验仍由 KB-07 的**分片预览 + 代价预估**承担（规范称其为「提交前唯一的质量校验手段」）。二者分工是——预览管「提交前参数是否合理」，检索验证管「构建后索引是否可用」。

**验收标准**（复核证据：`npm run verify:kb08` 含工具块与引用检查；`npm run verify:retrieval` 含检索验证页）

- [x] 工具返回的证据足以支撑带引用的回答。→ 每个命中含 `file` / `doc_id` / `ordinal` / `char_start` / `char_end` / `text` / `match_score` / `band`，渲染文本按 `N. [band score] file #ordinal (字符 a-b)` 逐行给出，模型据此可标注来源与定位。
- [x] 引用序号与来源一一对应，可在界面回溯。→ 会话内工具调用块展开后渲染引用列表：序号、文件名、段落序号 + 字符区间、置信度（文字承载）、分数。**注**：正文角标可点击回溯的方案（b）依赖模型配合输出 `[n]`，不可靠，故采用方案 (a)——在工具块内列出引用，用户在那里回溯。
- [x] 引用列表的分数与检索结果一致，不出现两套口径。→ 工具块解析的正是模型读到的那份渲染文本，因此界面呈现与模型所见**同源**；若两者不一致即为缺陷而非显示差异。
- [x] 命中分数低于阈值时不会出现模型自由发挥。→ 阈值在 `KnowledgeOperations.search` 内生效，低于阈值只计入 `below_floor` 而不返回；空结果返回「未找到依据 + 建议」的可判定文案。
- [x] 面板底部免责说明。→ **不适用**：插件不提供回答面板，免责说明属宿主会话或产品文案范畴。
- [x] 加载中、无引用、模型失败三种状态均有明确呈现。→ 加载中：摘要行显示「进行中」；无引用：块内标记「无命中」并给出下一步建议；失败：块内标记「失败」并保留原因。**模型失败**属会话范畴，不在插件内。
- [x] 不新增插件自带的问答 UI。→ 范围约束；插件仅有两处 UI：侧栏入口 + 主区面板、会话内工具调用块。**注**：面板内的「检索验证」Tab 不是问答 UI，它是诊断台（不生成回答），见上文恢复理由。
- [x] 恢复的检索验证页可判定检索质量。→ 可调 `minScore`/`topk`/稠密-混合模式；返回完整分片文本、字符区间、`match_score` 与 `band`、`below_floor` 与耗时、active slot/分片数/构建时间；只读且不计入 `hits7d`。验收 `npm run verify:retrieval`。

**KB-09 结论：闭环（8/8 可判定项；范围已修订两次并记录理由）。** 本 issue 的交付实质并入 KB-08 的会话内呈现，保留独立条目以维持追溯链。第二次修订恢复了检索验证页——原「不实现」的判断被真实使用证伪。

**不做**：模型侧微调；多轮对话记忆（设计规范未覆盖）；**插件自建的问答 UI（归 dsh 会话）**；检索验证页内的回答生成、多轮对话与参数 A/B 对比。

---

### KB-11 · 质量收口与交付验收

| 项 | 内容 |
|----|------|
| 优先级 | P1 |
| 类型 | 验收 |
| 依赖 | KB-01 … KB-10 |
| 依据 | 设计规范 4.7、8.1–8.3、7.6、9.4、10.2 |

**范围**

- **五态全覆盖**：所有数据驱动区域必须实现加载（骨架屏，形状与最终内容一致）、空状态（说明为什么为空 + 下一步 + 主操作入口）、错误态（失败原因 + 影响范围 + 恢复方式，禁止只显示错误码）、成功态（关键写操作完成后明确确认反馈）、受限态（权限或配额不足时说明限制来源与解除路径）。
- **可访问性**：焦点可见、键序与视觉顺序一致、纯图标按钮提供可访问名称、状态标记不靠颜色单独承载、尊重系统减少动效设置、对比度正文 ≥ 4.5:1、大字与图形 ≥ 3:1。
- **深色主题独立抽样**：语义文字与品牌文字对比度必须单独核对，**不得沿用浅色稿的检测结果**。
- **补齐 P2 / P3 组件**：P2（切分与嵌入策略配置器、构建记录表、混合检索权重）、P3（空状态、骨架屏、追问建议、计数徽标）。
- **从零安装与真实组合验证**：全新 `DSH_HOME` 与新 profile 安装 → 断言 profile 依赖与 bundle 列表 → 断言所有 exports / host+client bundle / patch / 静态资源存在 → `--dump-config` 出现插件层 → 启动后检查宿主 route、client 名册与真实 UI。
- **设计走查清单**：把交互与可访问性要求转为可自动或半自动检测的规则。

**验收标准**

- [x] 五态在页面中均可复现。→ **已按修订后的页面集核对**（总览 / 文档接入 / 索引策略三个页面；原清单中的「检索测试」「策略配置」两页已按 KB-09 的范围修订取消）：加载（骨架屏，形状与内容一致，`aria-busy`）✓；空状态（说明原因 + 下一步；**且「筛选后为空」与「真的没有数据」是两个不同状态**）✓；错误态（原因 + 重试，`role="alert"`，禁止只显示错误码）✓；成功态（构建完成写入 `log('success')`，失败写 `log('error')`）✓；**受限态（配额）✓ —— 见下方说明，此态此前不可达**。
- [x] 对比度抽检通过并留档，浅色与深色各一轮，附检测项清单。→ `npm run verify:contrast`，按 WCAG 2.x 相对亮度公式计算，**浅色与深色各自独立取样**（共 25 项配对 × 2 主题）。结果留档 `plugin/tmp/contrast-report.json`（含每对的比值、阈值与判定）。**浅色曾 4 处不达标**（见规范 v1.1.2 修订），修正后 **浅色 25/25、深色 25/25**。深色全过而浅色不达标，恰好印证了「不得沿用浅色检测结果」这条要求的必要性。
- [x] 全新 profile 安装后真实任务在 headless profile 下跑通。→ `npm run verify:profile`，**25 项**（原 14 项）：全新 `DSH_HOME` → 安装 → 断言 `dsh.profile.bundles` 含插件 → `--dump-config` 插件层完整（16 个配置键，含新增的 `quota` / `embedding`）→ **断言 dump 中不出现任何凭据形态字段**（API key 只以变量名出现）→ 断言已安装产物中的三处 slot 贡献、工具注册、`inject = ['tools']`、客户端 bundle 仅 import 模块表项、样式已内嵌、令牌产物可发布。**端到端真实检索**由 `npm run verify:e2e` 单独覆盖（需活的嵌入端点，无端点时干净跳过）。
- [x] `typecheck` / `build` / `verify` 与 `git diff --check` 全部通过。→ 以 `package.json.scripts` 实际声明为准（本包用 npm 而非 pnpm，原因见 README 与 MEMORY）：`npm run typecheck` exit 0；`npm run verify` 全链路 exit 0（**14 个套件、557 项断言**）；`git diff --check` 干净。
- [x] 走查清单条目全部可自动或半自动检测。→ `npm run verify:walkthrough`，**52 项**，把 §8.2–§8.4 与 §4.7 的条目逐条转为断言：焦点不可移除（扫描全部样式表，仅允许 `:focus:not(:focus-visible)` 这一种抑制）、纯图标控件必须有可访问名称（扫描全部 `.tsx`）、装饰性图标 `aria-hidden` 且 `focusable={false}`、状态不只靠颜色承载（胶囊必须有文字与描边）、拖拽区为真 button 且文件输入用 clip-path 视觉隐藏而非 `display:none`、选项卡方向键导航并在两端回绕、尊重 `prefers-reduced-motion`、按页唯一 primary、负向校验必须说明可接受范围（**实际调用校验函数断言消息内容**，不是读源码）。
  **「半自动」按字面执行**：机器能判的判，判不了的列出来。该套件**不声称**结果对人可用——对比度是另行的数值测量，读屏行为只在标记可判定的范围内断言。三条失败的检查最后发现是**检查本身写错**（详见下方），已修正检查而非绕过代码。
- [x] 未执行任何未经授权的 commit、push、发布或可见性变更。→ 全程仅在本地仓库 commit（用户显式要求「初始化 git 仓库」后）。**未执行任何 push、未发布到 npm、未变更任何仓库可见性**；设计规范的修订**也只在本地完成，未上传云端节点**（按用户「只在本地修改」的指示）。

**唯一一项此前不可达的验收项已按用户要求补齐：受限态。** 原状：规范 §8.2 要求「权限或配额不足时说明限制来源与解除路径」，但配额数值留待业务侧确认，插件也未强制任何配额——**该状态没有触发路径，因此既不可测也不算真正实现**。现实现为**可配置配额**（`quota.bytes` / `warnAt`，默认不限），并在**两个增长点**强制：接受上传时、以及构建时（按计划占用检查——只查上传会让重建把一个刚被拒的存储再翻一倍，配额会在它真正存在的场合失效）。拒绝消息含「被阻断的操作 + 数字 + 缺口 + 两条解除路径」。`npm run verify:quota` **42 项**，**以真实写满存储直到上传被拒的方式**验证该状态可达，并确认构建路径同样被拒、侧栏数字与强制逻辑同源。

**实施期修复的真实缺陷（记录备查）**

| # | 缺陷 | 发现方式 |
|---|------|----------|
| 1 | 受限态告警的严重度只判 `exceeded`，但**被拒的写入会让存储停在限额以下**（它正因会越线才被拒），于是动作被停止的那一刻反而显示平静提示 | 配额套件断言「被拒后界面为 alert」 |
| 2 | 检索工具的超时只在 `await embedQuery` 返回**之后**才检查 abort 标志，provider 若不接受 signal 则永久挂起——**超时永不触发** | KB-08 套件的超时用例直接挂死 |
| 3 | 空 `apiKeyEnv`（意为「无需鉴权」）被当作「变量缺失」，导致对本地端点的**每一次调用都被拒** | 接 Ollama 时第一次真实调用即失败 |
| 4 | 集合 schema 的向量维度写死 1024，而实际部署的模型返回 **2560** | 真实端点探测 |
| 5 | `DocumentRecord` 的模块注释声称存了文本，接口里却没有该字段（重建无从重读源文本） | 实施 KB-07 时发现 |
| 6 | 浅色主题 4 处对比度不达标，且均为**规范自己发布的色值** | 对比度审计 |

**KB-11 结论：闭环（6/6）。**

**不做**：任何新增功能。

---

## 5 全局验收基线（适用于全部 issue）

1. 颜色不兼职：品牌色只承担主操作、选中态与焦点态；语义色只承担状态表达。
2. 深度靠边框，不靠投影；阴影仅三级且只用于浮层，常规卡片不投影。
3. 状态不靠颜色单独承载：每个状态标记必须同时具备文字、实色与描边。
4. 技术值一律等宽：集合标识、分片数、分数、时间戳统一 JetBrains Mono 与等宽数字。
5. 不暴露内部数值：原始向量距离属实现细节，对外只呈现归一化分数与分档。
6. 禁止在业务样式中硬编码颜色与尺寸、使用令牌之外的中间色、通过提高选择器权重覆盖组件样式。
7. 界面术语必须与 zvec 实际模型对齐，不自造概念。

---

## 6 待决与阻塞项（不进入上述 issue 范围）

| # | 事项 | 当前取值 | 阻塞的 issue | 需谁确认 |
|---|------|----------|--------------|----------|
| 1 | 创建知识库分步向导 | **本期未纳入，用户明确暂缓** | 无（不排期） | 业务侧确认后另开 |
| 2 | 切分默认值 | 按标题层级 / 1024 / 128 | KB-07 | 业务侧：若面向代码库或短文本，应改为固定长度并下调分片长度 |
| 3 | 量化器默认 | **INT8**（规范原文记 PQ-INT8，实测引擎无 PQ） | KB-07、KB-10 | 业务侧：若召回率优先于存储，应关闭量化 |
| 4 | 索引类型默认 | HNSW | KB-07、KB-10 | 业务侧：规模达百万分片以上应改 IVF 或 DiskANN |
| 5 | 相似度阈值 | 0.55 | KB-08、KB-09 | 需按实际语料分布回归后确定 |
| 6 | 深色空态 / 加载 / 错误态视觉稿 | 未交付 | KB-11 | 设计侧：仅令牌与三张核心界面稿已交付 |

---

## 7 拆分时发现的设计规范内部不一致（**已全部处置，v1.1.1**）

以下问题会在实现阶段直接产生歧义。**六项均已随规范本地修订到 v1.1.1 而关闭**，处置逐条记录如下——保留原始现象描述，以便追溯这次修订到底改了什么。

| # | 位置 | 现象 | 处置（v1.1.1） |
|---|------|------|----------------|
| 1 | 3.7 尺度令牌 vs 4.1 / 4.2 / 4.3 | 令牌定义 `controlMd = 32`，但按钮 md 尺寸、文本框高度、下拉与分段控件高度均为 **34** | **已定：令牌改 34。** 规范 3.7 补入修订说明，并写明若日后判定应以 32 为准，须同步改 4.1/4.2/4.3 三处，不得只改一处（否则同一表单控件高度不一致，违反相邻条款） |
| 2 | 第 8 章 / 第 9 章 / 第 10 章 | 第 8 章小节编号为 `7.1–7.3`；第 9 章为 `8.1–8.4`；第 10 章为 `9.1–9.2` | **已归位**：第 8 章 `8.1–8.4`、第 9 章 `9.1–9.4`、第 10 章 `10.1–10.3`。版本表的修订清单登记了本次归位，正文交叉引用同步核对 |
| 3 | 3.6 标题 | 源文件出现 `### 3.6 字体令### 3.6 字体令牌`（标题重复粘连） | **已修复**：正文与 TOC 两处粘连标题均修正为 `### 3.6 字体令牌` |
| 4 | 3.1 vs 深色画板 | 文档记令牌 51 项（Color 22 + Scale 29）；深色画板记录为 53 项 | **已定并统一口径**：实际实现为 **148 项 / 17 组**（48 项有深色覆盖），规范 1.5 与 3.1 同步改写，逐项明细指向 `plugin/tokens/kb-tokens.json`。**并加 `verify:spec` 门禁**比对规范表述与实现是否一致，防再次漂移 |
| 5 | 3.2 vs 6.1、5.1 | 3.2 规定「分割线只使用 neutral-200」，但 6.1 顶栏用 `#E9ECF2`、5.1 卡片内分隔线用 `#EDEFF4`（均不在令牌表内） | **已定：纳入令牌表**。新增 `--kb-divider-topbar` / `--kb-divider-card` 两个具名令牌承载这两处实测值，不再算裸色值；3.2 的「只用 neutral-200」改为「默认使用 neutral-200，顶栏与卡片内分隔线使用各自具名令牌」 |
| 6 | 3.7 vs 4.4 / 4.5 / 4.6 / 5.2 | 标签高 26、胶囊高 26/22、徽标高 20、图标按钮 34×34、复选框 16×16、开关 38×22、进度条高 5、上传卡图标 38×38 等**组件级尺寸未纳入 Scale 令牌集** | **已定：扩充令牌集**（不放宽验收项）。新增 `component` 组（30 项）承载组件级尺寸，3.7 补入「Scale 与 component 的边界判据」。这使 KB-02「静态扫描无裸尺寸残留」在组件层可达成，而不是被迫放宽 |

> **结论**：第 1、5、6 项（取值层面的硬冲突）已按「实现侧取值更合理的一侧」定案并回写；第 2、3、4 项（文档一致性缺陷）已修正。规范版本号升至 **v1.1.1**，1.5 节含完整修订清单。**本地修订已完成，尚未上传云端**。

---

## 8 追溯矩阵（设计规范章节 → Issue）

| 规范章节 | 覆盖 issue |
|----------|------------|
| 1 文档说明 | — |
| 2 产品与设计目标 | KB-01、KB-05 … KB-09 |
| 3 设计令牌 | KB-02（3.1–3.9 全覆盖） |
| 4 基础组件规范 | KB-03（4.1–4.7） |
| 5 业务组件规范 | KB-05（5.1）、KB-06（5.2）、KB-07（5.3 / 5.5 / 5.6）、KB-08（5.4） |
| 6 页面规格 | KB-04（6.1）、KB-05（6.2）、KB-06+KB-07（6.3）、KB-08+KB-09（6.4）、KB-07（6.5） |
| 7 深色主题规范 | KB-02（令牌层）、KB-04 / KB-05 / KB-07 / KB-08 / KB-09（界面层）、KB-11（对比度验收） |
| 8 交互与状态规范（正文小节编号 7.x） | KB-11（五态、反馈确认、键盘与可访问性） |
| 9 工程落地对照（正文小节编号 8.x） | KB-01（9.2）、KB-02（8.3→9.3）、KB-03 … KB-08（9.4 优先级）、KB-10（9.1） |
| 10 待决事项与后续计划 | 第 6 节（待决）、KB-11（走查清单与 W3C JSON 接入） |

---

## 9 zvec 实测偏差（首次实施后回补，影响 KB-07 / KB-08 / KB-10）

在 `plugin/` 的首次实现中真机验证了 `@zvec/zvec` 0.7.1（win32-x64 绑定），发现设计规范的三处技术假设
与实际 API 不符。下列 issue 的验收标准需相应修正，否则实现会照着一个不存在的量化器和一个方向相反的
分数语义去写。

| # | 规范假设 | 实测结论 | 影响的 issue | 处置 |
|---|----------|----------|--------------|------|
| 1 | 命中分数由 zvec 距离归一化得到（§3.4 表述正确） | `querySync` 返回的 `score` 是**距离**（越小越相似），列表已按距离升序；查询向量等于某文档得 0，正交得 1 | KB-08、KB-09 | 规范化必须实现为 `matchScore = clamp(1 - distance)`，而非对原值 clamp。方向读反会把最差命中排在最前，并让全部命中越过 0.55 阈值 |
| 2 | 量化器「PQ-INT8（压缩 4×）」 | `ZVecQuantizeType` 只有 `FP16 / INT8 / INT4`，**不存在 PQ**；另有 RaBitQ 索引族 `HNSW_RABITQ` / `IVF_RABITQ` | KB-07 | 配置器选项与默认值改为 `INT8`；「压缩 4×」对应 FP32→INT8 的 4 倍压缩。若要用 RaBitQ，它属于索引类型而非量化器，UI 归属需重新划分 |
| 3 | 索引类型 HNSW / IVF / DiskANN | `ZVecIndexType` 实际含 `HNSW` / `IVF` / `FLAT` / `HNSW_RABITQ` / `DISKANN` / `IVF_RABITQ` / `INVERT` / `FTS` | KB-07 | 分段控件至少覆盖 HNSW / IVF / DiskANN；两个 RaBitQ 变体是否对用户暴露待业务确认 |
| 4 | 距离度量默认 cosine（§5.6） | HNSW / IVF / DiskANN / FLAT 的 `metricType` **默认值均为 IP（内积）** | KB-07、KB-10 | 必须按索引显式设置 `COSINE`，否则分数尺度与 0.55 阈值全部失准 |
| 5 | 集合可反复按建库方式打开 | `ZVecCreateAndOpen(path, schema)` 要求 `path` **不存在**；重开既有集合必须走 `ZVecOpen(path)`，且不接收 schema | KB-05、KB-10 | 集合打开分两条路径（新建 / 重开），实现需按存在性分派；schema 随集合持久化，不随打开传入 |

**已验证可用**：原生绑定在 Windows x86_64 上加载正常，HNSW + COSINE + `m=32` / `efConstruction=200`
被接受，写入、检索、关闭后重开数据一致，四档置信度映射（1.0 / 0.994 / 0.707 / 0）与余弦理论值吻合。
证据留档：`plugin/zvec-smoke-report.json`，可复跑命令 `npm run smoke:zvec`。

### 9.1 KB-10 实施期新增实测结论（影响 KB-05 / KB-07 / KB-08 / KB-09）

补充探测证据：`plugin/scripts/zvec-probe10.mjs`、`zvec-probe-filter.mjs`、`zvec-probe-lock.mjs`，
以及验收套件 `plugin/scripts/verify-kb10.mjs`（53 项，`npm run verify:kb10`）。

| # | 假设 / 直觉 | 实测结论 | 影响的 issue | 处置 |
|---|-------------|----------|--------------|------|
| 6 | 「路径已存在」会报 `ZVEC_ALREADY_EXISTS` | `ZVecCreateAndOpen` 对已存在路径抛 **`ZVEC_INVALID_ARGUMENT`**（消息为 `path validate failed: path[...] exists`），与「参数非法」同码 | KB-05、KB-10 | **不能靠错误码分派新建 / 重开**，必须按路径存在性判断。已按此实现（`store/snapshot.ts`） |
| 7 | 同一集合可被多次打开（含只读并发读） | 集合目录带**独占锁**：同一路径第二次 `ZVecOpen` 无论读写都失败（`ZVEC_INTERNAL_ERROR: Can't lock .../LOCK`）；关闭后可重开 | KB-08、KB-10 | 必须**按槽位池化句柄**并引用计数，不能按请求开闭。已实现 `store/registry.ts`；插件卸载 disposer 释放全部句柄，否则重载会撞锁 |
| 8 | 标量过滤用 SQL 风格 `==` | 过滤语言只接受**单等号 `=`**，`==` 直接语法报错；字符串用单引号，支持 `LIKE` / `IN` / `AND` / `BETWEEN`；`SELECT ... WHERE` 不被接受 | KB-06、KB-08 | 过滤表达式统一由 `documentFilter()` 生成并转义引号，禁止在别处手写 |
| 9 | 全文检索需自己实现融合 | 引擎自带 **FTS 索引**（`tokenizerName` 支持 `standard` / `ngram` / `jieba` / `whitespace`）与 **`multiQuerySync` + `rerank: { type: 'rrf' }`**；中文 `matchString: '检索'` 实测命中 | KB-07、KB-08 | 混合检索直接用引擎融合，不手写 RRF。**注意**：RRF 返回的是**排名融合分**（量纲取决于 `rankConstant`），不是相似度，不能当 `match_score` 用；已改为「保留融合排序 + 取该命中自身稠密距离归一化」 |
| 10 | 重建索引期间旧索引仍可读 | 引擎不支持同目录双开，故「旧快照继续服务」必须由**双槽位 + 指针切换**实现：写入非活动槽，发布时原子翻转 `meta.json` 的 `active` | KB-07、KB-10 | 已实现（`store/snapshot.ts`）。取消或失败只丢弃非活动槽，检索始终读活动槽，天然满足「重建中返回上一次快照」 |

**结论**：第 9 条推翻了「自己算 RRF 分数即可」的直觉，第 7 条推翻了「按请求开集合」的直觉——
两者都会在并发一上来时以锁错误或错误排序暴露，已在 KB-10 层一次性收口，KB-07 / KB-08 直接复用。
